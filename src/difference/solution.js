/** .........
 * Sum of two numbers.
 *
 * @param {number} num1 - The first number to subtract.
 * @param {number} num2 - The second number subtract from num1.
 * @returns {number} The difference between num1 and num2.
 */

export const solutionName = (num1, num2) => {
    return num1 - num2;
};
